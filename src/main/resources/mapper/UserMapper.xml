<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">
    <!-- 查询所有用户 -->
    <select id="selectAll" resultType="com.example.entity.User">
        SELECT * FROM user ORDER BY id DESC
    </select>

    <!-- 根据用户名查询用户 -->
    <select id="findByUsername" resultType="com.example.entity.User">
        SELECT * FROM user WHERE username = #{username}
    </select>
    
    <!-- 根据ID查询用户 -->
    <select id="findById" resultType="com.example.entity.User">
        SELECT * FROM user WHERE id = #{id}
    </select>
    
    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.example.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(username, password, email, phone, real_name, role) 
        VALUES(#{username}, #{password}, #{email}, #{phone}, #{realName}, #{role})
    </insert>
    
    <!-- 更新用户 -->
    <update id="update" parameterType="com.example.entity.User">
        UPDATE user SET 
            password = #{password}, 
            email = #{email}, 
            phone = #{phone},
            real_name = #{realName}, 
            avatar = #{avatar}, 
            status = #{status} 
        WHERE id = #{id}
    </update>
    
    <!-- 删除用户 -->
    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>
</mapper>